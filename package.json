{
  "name": "dcac-crm-dashboard",
  "version": "1.0.0",
  "engines": { "node": "18.x" },
  "scripts": {
    "build": "mkdir -p dist && cp public/index.html dist/index.html"
  },
  "dependencies": {
    "googleapis": "^140.0.0"
  }
}
